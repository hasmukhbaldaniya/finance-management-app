# gateway-service's browser-facing origin (see gateway-service/CLAUDE.md for
# its full routing table). Read in src/main/api/gateway.ts — every gateway
# HTTP call happens in the main process, never the renderer (see
# desktop/CLAUDE.md's "gateway bridge" section for why).
VITE_API_BASE_URL=http://localhost:4400/api
